# Experiment: test_resnet+swav_bn

This folder is tracked for experiment metadata.

Weights/checkpoints are intentionally excluded from git:
- `checkpoint.pth.tar`
- `queue0.pth`
- `checkpoints/*.pth`

Place your local weights in the same paths if you need to reproduce results.
